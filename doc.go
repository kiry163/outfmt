/*
Package outfmt provides a small rendering layer for Go CLI applications.

It converts structured data into table, YAML, or JSON output so the same data
can be consumed by both humans and agents. Table output is optimized for
terminal readability and can flatten nested struct and map fields into
dot-separated columns.
*/
package outfmt
